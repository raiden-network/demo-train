# File paths
keystore-path = './receiver/key_storage/'
password-file = './wallet_password.txt'

# Networking
matrix-server = "https://transport02.raiden.network"
eth-rpc-endpoint = "http://parity.kovan.ethnodes.brainbot.com:8545"
network-id = 42 

environment-type = "development"
log-config = "raiden:ERROR"
accept-disclaimer = true
